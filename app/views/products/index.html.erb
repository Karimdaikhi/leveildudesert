<div class="container marged-top" >
  <div class="row">
    <div class="col-xs-12 col-sm-6">
      <div class="produt-show">
        <%= image_tag @product.photo %>

    <div class="button-position">
      <h3 ><%= @product.name.upcase %></h3>
      <br>
      <p><%= @product.price %> €</p>
      <br>
      <a href="#" class="btn-medium">EN SAVOIR PLUS</a>
    </div>
    </div>

    </div>
    <div class="col-xs-12 col-sm-6">
      <div class="banner">
        <div class="banner-content" >
          <h1>NOUVEAUTÉ</h1>
          <p>Trouvez votre produit via un diagnostic personnalisé. Veuillez télécharger une photo depuis votre ordinateur.<br><br></p>
        <%= simple_form_for @profile, :url => profile_photo_path, :method => :post  do |f| %>
        <%= f.input :photo, label: "Charger ma photo" %>
        <%= f.input :photo_cache, as: :hidden %>
        <%= f.button :submit, "GO !", class: 'btn-medium' %>
    <% end %>
    </div>
  </div>
</div>



